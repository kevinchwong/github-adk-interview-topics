# MongoDB Configuration (Railway)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/interview_topics

# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Gemini Model Configuration
GEMINI_MODEL=gemini-pro

# Generation Parameters (Optional)
NUM_TOPICS=15
DIFFICULTY_FOCUS=mixed

# GitHub Actions Metadata (Automatically set in CI)
RUN_ID=local-development
GITHUB_SHA=local